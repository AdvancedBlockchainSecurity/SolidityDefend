# v2.0.14 Baseline

**Generated:** 2026-07-14 · **Tool:** `scripts/baseline.sh` · **Binary:** v2.0.14 · **Detectors:** 90
**Ground truth:** `tests/validation/ground_truth.json` v1.4.0 (122 contracts)

This is the canonical reference baseline. Regenerate with `scripts/baseline.sh`
(or `scripts/baseline.sh --json` for the machine-readable summary). The script
uses the tool's own `--validate` harness and `-o` file output only — it never
parses banner-wrapped stdout, so results are deterministic and reproducible.

## Overall metrics (authoritative, via `--validate`)

| Metric | Value |
|--------|-------|
| Files analyzed | 122 (0 failed) |
| True Positives | **149 / 149 (100.0%)** |
| False Negatives | 0 / 149 (0.0%) — no missed vulnerabilities |
| False Positives | 170 / 319 (53.3%) |
| **Recall** | **100.0%** |
| **Precision** | **46.7%** |
| F1 | 0.637 |

## Health invariants (enforced — script exits non-zero on violation)

| Invariant | Result |
|-----------|--------|
| Recall == 100% (no expected TP missed) | **PASS** |
| Clean-contract false positives == 0 | **PASS** |

## Clean contracts — 0 false positives

Every finding on an audited-safe contract is a false positive. All five stay silent:

| Findings | Target |
|---|---|
| 0 | `tests/contracts/clean_examples/clean_contract.sol` |
| 0 | `tests/contracts/fp_benchmarks/safe_amm_pool.sol` |
| 0 | `tests/contracts/fp_benchmarks/safe_chainlink_consumer.sol` |
| 0 | `tests/contracts/fp_benchmarks/safe_erc4626_vault.sol` |
| 0 | `tests/contracts/fp_benchmarks/safe_flash_loan_provider.sol` |
| **0** | **total clean FPs** |

## Vulnerable targets — findings census (144 total)

| Findings | Target |
|---|---|
| 2 | `basic_vulnerabilities/reentrancy_issues.sol` |
| 1 | `basic_vulnerabilities/validation_issues.sol` |
| 10 | `vulnerable/` |
| 2 | `flash_loans/` |
| 7 | `erc4626_vaults/` |
| 1 | `price-manipulation/` |
| 22 | `cross_chain/` |
| 41 | `delegatecall/` |
| 24 | `signatures/` |
| 13 | `specialized/` |
| 14 | `restaking/` |
| 4 | `account_abstraction/` |
| 3 | `amm_context/` |
| **144** | **total** |

## Precision debt — the 170 un-annotated findings

Every one of the 170 "false positives" lands on a **vulnerable** contract, not a
clean one (clean FPs = 0). They are findings the current 90-detector set produces
that `ground_truth.json` v1.4.0 (last refreshed 2026-02-16, pre-dating several
detectors) does not annotate as expected. They are **not** classic FPs on safe
code — they are the gap between "what the tool finds" and "what the dataset
records as expected." Sorted by contribution:

| FP | TP | Detector |
|---:|---:|----------|
| 72 | 0 | centralization-risk |
| 21 | 0 | block-dependency |
| 13 | 0 | gas-griefing |
| 12 | 2 | missing-access-modifiers |
| 11 | 1 | signature-replay |
| 11 | 0 | signature-malleability |
| 10 | 1 | classic-reentrancy |
| 6 | 0 | unchecked-external-call |
| 5 | 0 | delegatecall-user-controlled |
| 4 | 0 | dos-unbounded-operation |
| 3 | 0 | dos-external-call-loop |
| 1 | 11 | delegatecall-return-ignored |
| 1 | 0 | oracle-manipulation |
| **170** | | **total** |

**Reconciliation is the next step** to make precision meaningful again: triage
each cluster and either (a) annotate the legitimate ones as expected findings in
`ground_truth.json`, or (b) tighten the detector where the finding is a genuine
FP. Until then, treat **recall (149/149) and clean-FPs (0)** as the two hard
regression gates; precision (46.7%) is a recorded reference, not yet a target.

## Reproduce

```bash
scripts/baseline.sh          # human-readable report + invariant gate
scripts/baseline.sh --json   # machine-readable summary
```
